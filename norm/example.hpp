template <class T = void>
class ClassName
{
	private:
		int				_number;
		std::string		_first_name;

	protected:

	public:
		ClassName()
		:	_number(0),
			_first_name(NULL
			_first_name(NULL),
			_first_name(NULL),
			_first_name(NULL),
			_first_name(NULL),
			_first_name(NULL)
		{
			//thing if template file
		}

		oneMethod(void)
		{ } //empty

		int const &
		otherMethod(int nb)
		throw(throw type)
		{
			if (cond)
				//line
			if (cond)
			{
				//line
			}
			return ;
		}
};
